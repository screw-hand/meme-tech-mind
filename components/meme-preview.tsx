import { MemeSettingsType } from "@/types/meme-settings"

interface MemePreviewProps {
  settings: MemeSettingsType
}

export function MemePreview({ settings }: MemePreviewProps) {
  return <div>MemePreview</div>
}
